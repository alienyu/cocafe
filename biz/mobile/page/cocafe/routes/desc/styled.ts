import styled from 'styled-components';

const WrappedCmp = styled.div`
    background: #ffcc00;
    height: 100%;
    .frame {
        height: 100%;
        .text {
            width: 4.25rem;
            font-weight: 400;
            font-size: 0.32rem;
            color: #705000;
            line-height: 0.44rem;
        }
    }
`;

export {
    WrappedCmp
}